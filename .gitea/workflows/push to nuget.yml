name: Simple Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: one-runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run a one-liner script
        run: echo "Hello, World!"
