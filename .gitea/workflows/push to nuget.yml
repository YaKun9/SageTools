name: Simple Workflow

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: one-runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a one-liner script
        run: echo "Hello, World!"
