name: Simple Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: one-runner

    steps:
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Checkout code
        uses: actions/checkout@v2